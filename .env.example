# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=http://localhost:8000

# Push Notifications (VAPID)
# This should match the VAPID_PUBLIC_KEY from the backend
# Generate VAPID keys using: pnpm exec web-push generate-vapid-keys (in backend directory)
NEXT_PUBLIC_VAPID_KEY=your_vapid_public_key
